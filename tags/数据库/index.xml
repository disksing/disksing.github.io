<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>数据库</title><link>http://disksing.com/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/</link><description>我编程三日，两耳不闻人声，只有硬盘在歌唱。</description><language>zh-cn</language><generator>Hugo -- gohugo.io</generator><managingEditor>i@disksing.com (disksing)</managingEditor><webMaster>i@disksing.com (disksing)</webMaster><lastBuildDate>Thu, 06 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="http://disksing.com/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/" rel="self" type="application/rss+xml"/><item><title>数据库的外部一致性</title><link>http://disksing.com/external-consistency/</link><pubDate>Tue, 26 May 2020 00:00:00 +0000</pubDate><guid isPermaLink="true">http://disksing.com/external-consistency/</guid><description>&lt;![CDATA[<p>一言以蔽之，外部一致性就是事务在数据库内的执行序列不能违背外部观察到的顺序。</p><p>举个例子，客户端先创建一个事务写入一条数据，然后再创建一个事务读取刚才写入的数据，这时候理应能读到，数据库不能给返回个空，然后解释说我把读事务安排在写事务前面了，所以啥也没读到。</p><p>传统上数据库的 ACID 里面是没有这个概念的，即使是最高级别的<em>可串行化</em>也没有外部一致性的约束——它只规定了多个事务的运行结果跟一个一个依次运行的结果相同，但没有关于事务次序的规定。也就是说，理论上，对于所有的只读事务，数据库都可以直接返回空，然后解释说所有的只读事务都排在第一条写事务的前面。当然了，没有数据库真的会这么干。正常的数据库都会按照事务请求到达的次序来执行，这不仅符合正常业务的需求，而且在实现层面也容易做。</p><p>当数据库进入分布式时代之后，外部一致性这个问题才真正需要被考虑，外部一致性这个概念本身也是 Google Spanner 论文里最早提出来的。原因是在分布式数据通常会保存同一份数据的多份副本来保证高可用和容灾，当多个副本所在的多个节点同时提供服务时，我们需要应付副本同步所带来的复杂性。</p><p>比如事务在一个节点写入一条数据，完成后立即另启一个事务在另一个节点读取，能成功读到刚刚写入的数据吗？如果没读到，可以理解成在数据库层面，后一个事务先于前一个事务运行了，这样就违背了外部所观察到的顺序。</p><p>再举一个涉及 3 个事务的例子。第一个事务在一个节点写入数据 A，完成后再启动第二个事务在另一个节点写入数据 B，在这个过程中另有第三个并发运行的事务尝试读 A 和 B，如果它读到了 B 却没读到 A，那意味着第二个事务先于第一个事务运行了，也不满足外部一致性。</p><p>要注意的是，外部观察到两个事务有先后次序，一定是前一个事务完成后，后一个事务才开始。否则两个事务是并发的，数据库可以以任意顺序执行这两个事务。例如客户端先启动一个事务，在请求发往数据库之后再启动第二个事务，这时这两个事务就是并发的。即使是单机数据库，也不能保证第一个事务的请求会先于第二个事务到达服务器，这两个事务的执行顺序完全有可能调换。</p><p>有朋友可能注意到外部一致性跟分布式系统里的<em>线性一致性</em>很类似。没错其实是本质上是一回事，不过线性一致性一般针对单个 key 的场景，外部一致性更侧重于对比传统数据库系统的内部一致性（即事务的时序在数据库系统内部是自洽的）。</p><h2 id="tidb-的外部一致性">TiDB 的外部一致性</h2><p>TiDB 的方法是非常简单粗暴的，所有事务的 ts （用于标识事务的顺序）都要从中心节点 PD 获取，PD 在分配时保证 ts 严格单调递增。</p><p>因为所有事务都要通过同一个 PD 取 ts，假如在外部观察到两个事务有先后次序（如前所说，前一个事务提交完成后，第二个才启动），那么后面事务的 ts 一定会更大。于是，我们用 ts 的大小来规定事务的顺序，一定不会违背系统外部观察到的现象。</p><p>实际情况还要更复杂一点，因为事务往往涉及到多个节点，还需要使用 2PC 才能真正保证一致性，这里不展开了。</p><h2 id="spanner-的外部一致性">Spanner 的外部一致性</h2><p>Spanner 最广为人知的就是它使用了原子钟进行授时。但实际上原子钟只是手段，真正有开创意义的是 TrueTime。利用各种硬件设备（大多数情况下主要起作用的其实是 GPS）和算法，TrueTime API 可以对外返回当前估算时间及误差范围，事务逻辑在考虑到误差之后进行一些补偿，最后就能实现外部一致性了。</p><p>原理其实比较简单，打个比方说明下：你跟妹子约会，商量好了 12 点整在电影院门口碰头，结果你等到 12 点还没见到人。这时候你是不会直接离开的，因为你会想可能是两人表的时间没对准，在妹子看来还没到 12 点。然后你一直等到 12:30，发现妹子还没来，你就知道自己是被放鸽子了，毕竟表不准也不太可能差这么多。更进一步，假如你能精确知道误差范围，譬如说误差不超过 10 分钟，那么你等到 12:10 就能知道肯定等不来人了。</p><p>Spanner 的事务正是这么做的，核心点就是事务提交的时候，会等待误差范围那么长的时间，然后才给客户端返回。这样一来，客户端接着再启动事务，或者客户端用某种方式通知另一个机房的客户启动事务（即使使用量子通信），新事务的取到的时间一定会比前面那个事务提交的时间要晚。</p><h2 id="cockroachdb-的外部一致性">CockroachDB 的外部一致性</h2><p>CockroachDB 用的是 HLC（混合逻辑时钟），使用的是结合物理时钟和逻辑时钟的时间戳。这个其实是权衡之后的方案：主打场景是类似 Spanner 那样的全球化部署，但是作为开源方案，也不可能用专有设备来搞一套 TrueTime。如果退而求其次用 NTP 的话，时钟误差无法控制下来会很大程度上影响性能。</p><p>HLC 是怎么一回事呢？简单理解下，就是两个事务如果时间间隔大的话，用物理时间能进行定序，如果物理时间上很接近，就依靠逻辑时钟来进行排序。</p><p>逻辑时钟的工作方式是，每当两个节点进行通信（比如收发消息包，本质上是产生信息交换时），都进行一次时钟同步。比如 A 给 B 发消息，带上自己的时钟 100，B 收到消息后发现自己的时钟是 80，此时 B 就会把自己本地的时钟设为 101，以此来保证不同节点事件时间戳的大小能体现因果关系。</p><p>我们用前面举的例子分析一下。</p><p>第一个事务先在节点 A 写，完成后第二个事务去节点 B 读同一份数据，如果 B 是可读的，那一定意味着在这之前 A 和 B 之间发生过信息交换（可以想象一下 paxos 或者 raft），而信息交换会触发逻辑时钟同步，这样第二个事务在 B 所取到的时间戳一定大于第一个事务在 A 取到的时间戳，于是外部一致性就得到保证了。</p><p>不过 HLC 也不是绝对安全的。对于前面说的那个 3 事务的例子，CockroachDB 就可能破坏一致性。究其原因，两个写事务写的数据是没有交集的，因此它们对应的两个节点之间没有进行时钟同步，于是有可能后面的事务取到的时间戳更小。这个其实是逻辑时钟最主要的缺陷：逻辑时钟依赖于追踪系统内事件的因果关系，如果因果关系不在系统内部，就无能为力了。</p><hr><h3 id="参考资料">参考资料</h3><ul><li><a href="https://www.cockroachlabs.com/blog/consistency-model/">CockroachDB’s Consistency Model</a></li><li><a href="https://cse.buffalo.edu/tech-reports/2014-04.pdf">HLC paper</a></li><li><a href="https://static.googleusercontent.com/media/research.google.com/zh-CN//archive/spanner-osdi2012.pdf">Spanner paper</a></li></ul>
]]></description></item><item><title>分布式事务的 Commit Point</title><link>http://disksing.com/txn-commit-point/</link><pubDate>Sat, 22 Feb 2020 00:00:00 +0000</pubDate><guid isPermaLink="true">http://disksing.com/txn-commit-point/</guid><description>&lt;![CDATA[<p>要说这个数据库事务啊，讲究的是<a href="https://en.wikipedia.org/wiki/ACID">ACID</a>。在分布式场景下，这四个没有一个是简单的，今天我们的话题主要涉及到 A(tomic)。</p><h2 id="一分布式环境的复杂性">一、分布式环境的复杂性</h2><p>在单机环境下，实现事务原子性并不复杂。一般的做法是事务提交之前的写入被存放在<em>预写式日志</em> 中，然后在事务提交时，往磁盘追加一条<em>提交记录</em>，完成事务的提交。</p><p>所谓<em>Commit Point</em>，在这个场景下指的是<em>提交记录</em> 被持久化到磁盘的一瞬间。在此之前，整个事务的写入都是未生效的状态，事务提交可能被回滚或中止（即使客户端已经发送了<em>Commit</em> 命令，数据库可能在<em>Commit Point</em> 之前崩溃）；而在<em>Commit Point</em> 之后，整个事务就被提交成功了（即使由于数据库崩溃没来得及把结果返回给客户端）。</p><p><strong>本质上，<em>Commit Point</em> 通过把事务内的多条 SQL 语句或者说多个对象的更新是否被提交“归约”到一个单点，也就是事务的<em>提交记录</em>，从而确保了“要么同时提交，要么同时回滚”。</strong></p><p>在分布式事务中，一个事务会同时牵扯到多个节点。这可能是因为事务本身要更新保存在不同节点的多个对象，也可能因为数据和索引保存在不同的节点（Global Index）。如果沿用单机数据库的经验，通过存储引擎中的<em>提交记录</em> 来照葫芦画瓢，很容易出现原子性被破坏的情况：</p><ul><li>部分节点成功提交，而部分节点由于冲突等原因需要回滚</li><li>部分节点成功提交，部分节点由于网络中断或崩溃无法提交</li><li>客户端与部分节点网络中断</li><li>客户端在向部分节点发送请求后崩溃</li></ul><h2 id="二两阶段提交">二、两阶段提交</h2><p>两阶段提交（2PC）引入了<em>协调者（coordinator）</em> 的角色，它通常以库的形式嵌入在发起事务的进程中，也可以以单独的进程或服务存在——这种情况下通常被称为<em>事务管理器（transaction manager）</em>。同时我们把持有数据的存储节点称为<em>参与者（participant）</em>。</p><figure style="text-align:center"><img src="/assets/img/txn-commit-point-1.png"/><figcaption align="center"><h6>两阶段提交（来源：设计数据密集型应用）</h6></figcaption></figure><p>所谓两阶段，是<em>协调者</em> 在提交的过程中，分两个步骤分别与<em>参与者</em> 交互：</p><ol><li>发送<em>准备（prepare）</em> 请求给所有<em>参与者</em> ，询问是否可能提交。</li><li>如果所有<em>参与者</em> 都回复<code>YES</code>，则发起第 2 阶段的<em>提交（commit）</em> 真正提交；如果任意一个<em>参与者</em> 回复<code>NO</code>或者超时无响应，则第 2 阶段改为<em>中止（abort）</em> 回滚之前的操作。</li></ol><p>这个过程类似西方婚礼时的流程。神父在第一阶段询问新娘和新郎是否要结婚，如果新娘和新郎都回复<code>YES</code>，神父才进入第二阶段，宣布二人结为夫妻。如果任意一个人说<code>NO</code>，结婚就中止了。</p><p>很显然，这个流程能防止“部分节点由于冲突等原因需要回滚”，但是并不能防住由于崩溃或者网络中断导致的不同步。好比神父在宣布二人结为夫妻时，新郎由于心情激动晕倒了没有听到，他醒来以后结婚还是未完成的状态，原子性就这样被破坏了。</p><p>解决这种“不同步”的关键正是<em>Commit Point</em>，正如前面所说，我们需要把多个对象的更新归约到一个单点。实践中，<em>Commit Point</em> 可以有多种选择，下面我们来逐一分析：</p><h3 id="方案一协调者-写入-提交记录-时为-commit-point">方案一：<em>协调者</em> 写入<em>提交记录</em> 时为<em>Commit Point</em></h3><p>这是很自然的想法：既然事务的的流程由单一的<em>协调者</em> 控制的，那么直接把单机的<em>提交记录</em> 引入<em>协调者</em> 就可以了。<em>协调者</em> 在发起第二阶段<em>提交</em> 之前，先在本地记录一份<em>提交记录</em>，<em>提交记录</em> 被持久化标志着整个事务提交成功，即<em>Commit Point</em>。</p><p>对于<em>参与者</em> 来说，当其在第一阶段回复<code>YES</code>后，就进入了<em>存疑（uncertain）</em> 状态，直到收到<em>协调者</em> 发来的第二阶段请求。在<em>存疑</em> 状态时，整个事务可能提交了也可能没有提交（由<em>Commit Point</em> 决定），也有可能事务已经被 abort。</p><p>回到婚礼的情景。这个方法就相当于神父在宣布结果之前，先记在一个小本本上，新娘新郎有没有结婚成功，完全以小本本上的记录为准。这个过程中不管是新人一方晕倒了或者是神父晕倒了，甚至大家全都晕倒了，只要小本本还在，所有人都能达成一致意见。</p><p><a href="https://en.wikipedia.org/wiki/X/Open_XA">X/Open XA 事务</a> 就是采用的这种方式。这个方案依赖于可靠的<em>协调者</em>，需要保证即使发生崩溃也要能恢复服务并且不丢失数据，因此往往会做成独立的高可用的<em>事务管理器</em> 服务，形成集群的单点。</p><h3 id="方案二所有-参与者-完成第一阶段时为-commit-point">方案二：所有<em>参与者</em> 完成第一阶段时为<em>Commit Point</em></h3><p>这是一个“去中心化”的思路，想法是这样的：在依赖<em>协调者</em> 写<em>提交记录</em> 的方案中，当所有<em>参与者</em> 都回复<code>YES</code>后，只要<em>协调者</em> 不出意外，总是会写入提交成功的记录，因此<em>Commit Point</em> 可以往前推，变成“所有<em>参与者</em> 都回复<code>YES</code>”。</p><p>在婚礼的例子里，这个方法就是说结婚成功与否并不取决于神父或者神父的小本本，而是由新娘新郎是否都回答了<code>YES</code> 唯一决定。只要两人都记得当时自己回答了什么并且不撒谎，不管婚礼现场发生了什么意外，事后大家把两人找出来重新问一下就可以了。</p><p>据不可靠消息 OceanBase 采用的正是这种方式。此方案的优点是延迟最小，因为不需要等待<em>协调者</em> 持久化<em>提交记录</em> ，但是当发生异常时恢复成本比较高，因为需要询问所有的<em>参与者</em>。另一个设计上的关键点在于由谁来维护事务的<em>参与者</em> 列表：如果由<em>协调者</em> 来负责，与前一种方案类似，必须要确保<em>协调者</em> 的可靠性；也可以把列表在第一阶段同步给所有的<em>参与者</em>，这时付出的代价是增加了<em>参与者</em> 要持久化的数据量。</p><h3 id="方案三某一特殊-参与者-完成第二阶段时为-commit-point">方案三：某一特殊<em>参与者</em> 完成第二阶段时为<em>Commit Point</em></h3><p>最后一种方法是从<em>参与者</em> 中选出一个来作为<em>Primary</em>，在提交的第二阶段<em>协调者</em> 会先把请求发给<em>Primary</em>，以 Primary 提交成功作为整个事务的<em>Commit Point</em>。相比于第一个方案，这个方法也有“去中心化”的效果，不过<em>Commit Point</em> 往后推迟了。</p><p>还是回到婚礼。婚礼之前，大家商量好了结果以新娘（<em>Primary</em>）是否收到神父的宣告为准，如果出现任何异常，只要新郎记得<em>Primary</em> 是新娘，到时候直接去问新娘就行。</p><p>TiDB 采用的就是这个方式。这个方案的优点是去中心化，不需要考虑<em>协调者</em> 的可靠性，而且带给<em>参与者</em> 的附带成本较低（只用记录<em>Primary</em> 而不是所有<em>参与者</em> ）。缺点就是延迟在三个方案中是最高的。</p><h2 id="三处理-存疑-状态">三、处理<em>存疑</em> 状态</h2><p>最后我们讨论 2 个关于<em>存疑</em> 状态的小问题。</p><h3 id="1-恢复中断的事务">1. 恢复中断的事务</h3><p>如前所述，当<em>参与者</em> 回复第一阶段消息后就进入<em>存疑</em> 状态，直到收到第二阶段的消息为止。在此期间由于无法知道事务提交成功还是失败，事务涉及的数据会进入 block 状态无法响应读写。因此不论<em>Commit Point</em> 如何选择，都必须要考虑中断的事务怎么恢复。</p><figure style="text-align:center"><img src="/assets/img/txn-commit-point-2.png"/><figcaption align="center"><h6>协调者崩溃（来源：设计数据密集型应用）</h6></figcaption></figure><p>对于方案 1，因为事务的<em>提交记录</em> 和<em>参与者</em> 列表都在<em>协调者</em> 持久化了，很显然应该由<em>协调者</em> 在恢复后负责恢复。具体地，如果已经写入<em>提交记录</em> 了，那么应该向所有<em>参与者</em> 发送<em>Commit</em> 消息；反之说明还没有过<em>Commit Point</em>，此时可以选择直接 abort 事务，如果<em>协调者</em> 保存了完整的事务信息，也可以重新从第一阶段开始两阶段提交，不过要注意这么做需要<em>参与者</em> 支持接收重复的<em>准备</em> 请求（幂等性）。</p><p>再看方案 3。由于<em>协调者</em> 不持久化任何事务状态，中断事务的恢复应该由<em>参与者</em> 负责。简单地说，处于<em>存疑</em> 状态的<em>参与者</em> 可以通过查询<em>Primary</em> 来获知事务的状态来决定把自身的<em>存疑</em> 调整为<em>已提交</em> 或者<em>已中止</em>。TiDB 中，中断事务的恢复是惰性的 ，<em>参与者</em> 不会主动推进事务，而是当有新事务访问到<em>存疑</em> 状态的数据时，新事务的<em>协调者</em> 客串一下原事务的<em>协调者</em> 来进行恢复。</p><p>至于方案 2 就比较灵活了，既可以使用方案 1 的方式也可以使用方案 3 的方式，取决于<em>参与者</em> 列表是保存在<em>协调者</em> 还是<em>参与者</em>。</p><h3 id="2-应该如何回复客户端">2. 应该如何回复客户端</h3><p>这个是在 TiDB 的开发中遇到的，仅以 TiDB 为例说明，其他的方案应该也会存在类似的问题。</p><p>在 TiDB 的事务模型里，tidb-server 和 tikv-server 分别扮演<em>协调者</em> 和<em>参与者</em> ，当 tidb-server 发出<em>CommitPrimary</em> 请求之后就进入了<em>存疑</em> 状态，此时如果 tikv-server 崩溃或者与 tidb-server 网络断开，tidb-server 就无从得知事务是否提交了。</p><p>这种情况下，tidb-server 既不能给客户端返回成功（可能<em>CommitPrimary</em> 并没有被 tikv-server 收到），也不能给客户端返回错误（可能已经过了<em>Commit Point</em>，但客户端会误认为事务失败）。此时只有两种办法，要么什么也不做等客户端自行超时，要么直接断开客户端的连接。TiDB 的做法是在一段时间内不断重试，寄希望于能恢复与 tikv-server 的连接，如果经过长时间重试仍然无法解除<em>存疑</em> 状态，最后就会直接断开连接来把<em>存疑</em> 的状态给传导给客户端。</p><p>值得注意的是，客户端需要处理事务的<em>存疑</em> 状态，这并不是分布式数据库所特有的问题。即使是单机的 MySQL，也存在“客户端发送<em>Commit</em> 命令的一瞬间网络断开”的可能，此时客户端同样无从判断服务端的情况从而进入<em>存疑</em> 状态。</p><hr><p>参考：</p><ul><li><a href="https://legacy.gitbook.com/book/vonng/ddia-cn/details">设计数据密集型应用</a></li></ul>
]]></description></item><item><title>理解 Google F1: Schema 变更算法</title><link>http://disksing.com/understanding-f1-schema-change/</link><pubDate>Wed, 18 Nov 2015 00:00:00 +0000</pubDate><guid isPermaLink="true">http://disksing.com/understanding-f1-schema-change/</guid><description>&lt;![CDATA[<p><em>注：本文已被<a href="https://github.com/ngaut/builddatabase">《从零开始写分布式数据库》</a>一书收录。</em></p><h2 id="背景">背景</h2><p>F1 是 Google 开发的分布式关系数据库，主要服务于 Google 的广告系统，它提供强一致性、高可用性，并支持传统 SQL 查询，近来也常常被称之为所谓的 NewSQL。</p><p>F1 是构建于 Spanner 之上的。Spanner 是 Google 开发的全球级数据存储引擎，它保证了数据存储的一致性和可用性，还通过 2PC（两阶段提交）提供了分布式事务读写。在分析 F1 时，我们可以简单地认为 Spanner 是一个全球分布的 kv 数据库。</p><p>F1 系统运行时由多台独立的 F1 服务器组成，为了保证整个系统的高可用性，F1 服务器被设计为无状态的，而且不存储数据——节点可以随时上线下线，客户端可以连接至任意节点发送请求。F1 服务器主要职能是将 RDBMS 中的结构化数据映射为可存储于 Spanner 的 kv 对，同时将客户端的 SQL 请求翻译成<em>get, set, del</em> 等简单的 kv 操作。</p><p>Schema 也就是关系数据库中表、列、索引、约束等定义，对应于 SQL 中的 DDL。很显然，Schema 决定了 F1 服务器的具体工作方式，客户端请求的解析和验证由 Schema 决定，之后如何翻译成 kv 操作也由 Schema 决定，Schema 可以被认为是 F1 服务器运行时所依赖的元信息。实践中，F1 服务器运行时自身会缓存一份 Schema 并有一定的机制保持定时更新。</p><p>F1 中的 Schema 变更是在线的、异步的，Schema 变更的过程中所有数据保持可用，保持数据一致性，并最大限度的减小对性能的影响。最大的难点在于所有 F1 服务器的 Schema 变更是无法同步的，也就是说不同的 F1 服务器会在不同的时间点切换至新 Schema。</p><p>由于所有的 F1 服务器共享同一个 kv 存储引擎，Schema 的异步更新可能造成严重的数据错乱。例如我们发起给一次添加索引的变更，更新后的节点会很负责地在添加一行数据的同时写入一条索引，随后另一个还没来得及更新的节点收到了删除同一行数据的请求，这个节点还完全不知道索引的存在，自然也不会去删除索引了，于是错误的索引就被遗留在数据库中。</p><h2 id="算法思想">算法思想</h2><p>为了更好地阐释算法思想，本节我们以一个简化的情境做类比。</p><p>假想我们有一家跨国公司，公司员工分布在全球各地，员工之间以电子邮件的方式互相通信，同时工作的性质要求员工之间发送的消息不能出现丢失。之后在某一天出现这样的需求：管理层决定把员工通信方式由邮件改为 QQ。</p><p>对于这样一个跨国公司来说，我们无法瞬间把新的工作方式通知给所有员工。假如先收到通知的员工先改为用 QQ 了，而未收到通知的员工还在用邮件，这样一来自然就会发生大量的消息丢失。</p><p>那么我们能不能通知员工在未来的某个时刻统一换用 QQ 呢？仔细一想也是不行的。因为每个员工的手表不可能是完全对准的，总是有的快点有的慢点，只要不能保证所有员工的时间完全校准，总有那么一个不一致的时刻。</p><p>下面让我们来看看 Google 的工程师们是怎么解决这个棘手的问题的。</p><h4 id="在未知中构建已知">在未知中构建已知</h4><p>根据上面的讨论，最根本的难点在于无法保证所有员工同时改变通信方式，这基本上是不可能做到的。本来员工都在用邮件，一旦通知发布出去，员工的工作方式就完全是未知的了——在任意一个时刻，任意一个员工都可能收到过通知而换用 QQ，也可能没收到通知而继续用邮件。</p><p>如果从现实层面来考虑，员工即使是离得远些，一段足够长的时间以后也应该能收到通知。员工的时间即使是没校准，也不至于错得太离谱。所以我们可以认为在足够长的时间过后，所有员工应该都已经换用 QQ 了。</p><p>我们可以使用一系列方案使“足够长的时间”变成“确定长度的时间”。首先，公司创建一个网站张贴最新的员工手册，其中自然包含员工应使用的通信方式等细则。其次，在员工入职时进行培训，要求员工每隔 30 分钟必须上网查看一下员工手册，并依照最新的手册行事。另外，如果出现网络故障等情况，员工在尝试访问网站 10 分钟后还没有看到新的手册，必须立即停止工作，直到重新看到手册为止。</p><p>基于这些规定，我们就至少可以知道从通知发布之后的某个时刻开始，所有工作中的员工都已经更新自己的通信方式了。例如我们中午 12:00 在网站上张贴新的手册，员工从 12:00 到 12:30 开始陆续查看手册并换用 QQ，到 12:30 时所有员工都应该尝试过访问网站了，到 12:40 时所有未能看到手册的员工都已经停止了工作，这时我们就可以认为所有工作中的员工都在用 QQ 了。如果再考虑手表时快时慢等特殊情形，我们不妨再多等 20 分钟。到了 13:00，我们可以非常自信地说：现在所有员工都换用 QQ 了。</p><p>在此基础之上，我们再规定两次修改员工手册的时间间隔不能少于 1 小时。例如在 QQ 之后我们还想换用微信，那么最早只能在 13:00 发布新的员工手册。根据前面的讨论，13:00 已经没有员工用邮件了，所以在整个演化过程中，有些时刻邮件和 QQ 同时被使用，有些时刻 QQ 和微信同时被使用，但一定不会发生邮件和微信同时被使用的情况。也就是说，在员工手册不断更新的过程中，最多只有两份手册生效：最新发布的这一份以及上一份。</p><h4 id="在矛盾中构建一致">在矛盾中构建一致</h4><p>上面我们设计了大量的规定和方案，最后只得到了不那么强的结论，看起来对问题的解决并没有什么帮助。不难发现问题的关键在于邮件和 QQ 这两种通信方式是矛盾不兼容的，只要有一个时刻有员工用邮件而有员工用 QQ，那么就很可能会造成消息丢失。</p><p>那么问题的本质其实是：在通信方式由邮件变成 QQ 的过程中，邮件和 QQ 这两种通信方式不能同时生效。</p><p>请回想一下上一节中我们得到过的结论，邮件和微信一定不可能同时被使用……你想到了吗？</p><p>BING！没错，只要我们在邮件和 QQ 中间加入一个其他的通信方式 X 作为过渡，因为同时只有两种连续的手册生效，邮件和 QQ 就很自然地被隔离了。很显然通信方式 X 一定不是微信，它一定是同时与邮件和 QQ 兼容的，在这里 X 的定义可以是：同时从邮件和 QQ 查收消息，发送消息时邮件和 QQ 各发送一遍。</p><p>以上就是 F1 Schema 变更的主要思想了。具体在 F1 Schema 变更的过程中，由于数据库本身的复杂性，有些变更无法由一个中间状态隔离，我们需要设计多个逐步递进的状态来进行演化。万变不离其宗，只要我们保证任意相邻两个状态是相互兼容的，整个演化的过程就是可依赖的。</p><h2 id="f1-中的算法实现">F1 中的算法实现</h2><h4 id="租约">租约</h4><p>F1 中 Schema 以特殊的 kv 对存储于 Spanner 中，同时每个 F1 服务器在运行过程中自身也维护一份拷贝。为了保证同一时刻最多只有 2 份 Schema 生效，F1 约定了长度为数分钟的 Schema 租约，所有 F1 服务器在租约到期后都要重新加载 Schema。如果节点无法重新完成续租，它将会自动终止服务并等待被集群管理设施重启。</p><h4 id="中间状态">中间状态</h4><p>前面已经提过，F1 在 Schema 变更的过程中，会把一次 Schema 的变更拆解为多个逐步递进的中间状态。实际上我们并不需要针对每种 Schema 变更单独设计中间状态，总共只需要两种就够了：<em>delete-only</em> 和<em>write-only</em> 。</p><p><em>delete-only</em> 指的是 Schema 元素的存在性只对删除操作可见。</p><p>例如当某索引处于<em>delete-only</em> 状态时，F1 服务器中执行对应表的删除一行数据操作时能“看到”该索引，所以会同时删除该行对应的索引，与之相对的，如果是插入一行数据则“看不到”该索引，所以 F1 不会尝试新增该行对应的索引。</p><p>具体的，如果 Schema 元素是<em>table</em> 或<em>column</em> ，该 Schema 元素只对<em>delete</em> 语句生效；如果 Schema 元素是<em>index</em> ，则只对<em>delete</em> 和<em>update</em> 语句生效，其中<em>update</em> 语句修改<em>index</em> 的过程可以认为是先<em>delete</em> 后再<em>insert</em> ，在<em>delete-only</em> 状态时只处理其中的<em>delete</em> 而忽略掉<em>insert</em> 。</p><p>总之，只要某 Schema 元素处于<em>delete-only</em> 状态，F1 保证该 Schema 元素对应的 kv 对总是能够被正确地删除，并且不会为此 Schema 元素创建任何新的 kv 对。</p><p><em>write-only</em> 指的是 Schema 元素对写操作可见，对读操作不可见。</p><p>例如当某索引处于<em>write-only</em> 状态时，不论是<em>insert</em> 、<em>delete</em> ，或是<em>update</em> ，F1 都保证正确的更新索引，只是对于查询来说该索引仍是不存在的。</p><p>简单的归纳下就是<em>write-only</em> 状态的 Schema 元素可写不可读。</p><h4 id="示例推演">示例推演</h4><p>Google 论文的叙述过程是描述完两种中间状态后就开始了冗长的形式化推导，最后得以证明按照特定的步骤来做 Schema 演化是能保证一致性的。这里我想先拿出一个例子把 Schema 变更的过程推演一遍，这样形成整体印象后更有助于看懂证明：）我们以添加索引为例，对应的完整 Schema 演化过程如下：</p><pre><code>absent --&gt; delete only --&gt; write only --(reorg)--&gt; public</code></pre><p>其中<em>delete-only</em> 和<em>write-only</em> 是介绍过了的中间状态。<em>absent</em> 指该索引完全不存在，也就是 Schema 变更的初始状态。<em>public</em> 自然对应变更完成后就状态，即索引可读可写，对所有操作可见。</p><p><em>reorg</em> 指“database reorganization”，不是一种 Schema 状态，而是发生在<em>write-only</em> 状态之后的一系列操作。这些操作是为了保证在索引变为<em>public</em> 之前所有旧数据的索引都被正确地生成。</p><p>根据之前的讨论，F1 中同时最多只可能有两份 Schema 生效，我们逐个步骤来分析。</p><p>先看<em>absent</em> 到<em>delete-only</em> 。很显然这个过程中不会出现与此索引相关的任何数据。</p><p>再看<em>delete-only</em> 到<em>write-only</em> 。根据<em>write-only</em> 的定义，一旦某节点进入<em>write-only</em> 状态后，任何数据变更都会同时更新索引。当然，不可能所有节点同时进入<em>write-only</em> 状态，但我们至少能保证没有节点还停留在<em>absent</em> 状态，<em>delete-only</em> 或<em>write-only</em> 状态的节点都能保证索引被正确清除。于是我们知道：从<em>write-only</em> 状态发布时刻开始，数据库中不会存在多余的索引。</p><p>接下来是<em>reorg</em> ，我们来考察<em>reorg</em> 开始时数据库的状态。首先因为<em>delete-only</em> 的存在，我们知道此时数据库中不存在多余的索引。另外此时不可能有节点还停留在<em>delete-only</em> 状态，我们又知道从这时起，所有数据的变更都能正确地更新索引。所以<em>reorg</em> 要做的就是取到当前时刻的 snapshot，为每条数据补写对应的索引即可。当然<em>reorg</em> 开始之后数据可能发生变更，这种情况下底层 Spanner 提供的一致性能保证<em>reorg</em> 的写入操作要么失败（说明新数据已提前写入），要么被新数据覆盖。</p><p>基于前面的讨论，到<em>reorg</em> 完成时，我们的数据不少不多也不错乱，可以放心地改为<em>public</em> 状态了。</p><h4 id="证明过程简介">证明过程简介</h4><p>这里简单介绍下证明过程，以理解为主，详细情况可自行查阅论文。</p><p>我们定义<strong>数据库表示</strong>为存储引擎中所有 kv 对的集合。<strong>数据库表示<em>d</em> 对于 Schema<em>S</em> 是一致的</strong>，当且仅当</p><ol><li><em>d</em> 中不存在多余数据。</li><li><em>d</em> 中的数据是完整的。</li></ol><p>其中不存在多余数据要求：</p><ol><li><em>d</em> 中的列数据或索引必须是 S 中定义过的列或索引。</li><li><em>d</em> 中所有索引都指向合法的行。</li><li><em>d</em> 中不存在未知数据。</li></ol><p>数据的完整性要求：</p><ol><li><em>public</em> 状态的行或索引是完整的。</li><li><em>public</em> 状态的约束是满足的。</li></ol><p>我们要求正确实现所有<em>delete, update, insert</em> 或<em>query</em> 操作，保证其对于任何 Schema<em>S</em> ，都不破坏数据库表示的一致性。</p><p>我们定义<strong>Schema<em>S1</em> 至<em>S2</em> 的变更过程是保持一致的</strong>，当且仅当</p><ol><li>任何<em>S1</em> 所定义的操作<em>OPs1</em> 都保持数据库表示<em>d</em> 对于<em>S2</em> 的一致性。</li><li>任何<em>S2</em> 所定义的操作<em>OPs2</em> 都保持数据库表示<em>d</em> 对于<em>S1</em> 的一致性。</li></ol><p>这里看起来第 2 点是没必要的，但确实是必须的，因为 F1 中在变更发生的过程中<em>S1</em> 和<em>S2</em> 是会同时生效的。我们考虑为<em>table</em> 添加一列<em>optional</em> 列<em>C</em> 的变更（<em>optional</em> 表示允许该列在数据库表示<em>d</em> 中不存在，对应于 SQL 中未定义<em>NOT NULL</em> 或有<em>DEFAULT</em> 的情况）。首先，<em>S2</em> 定义的<em>insert</em> 操作会写入<em>C</em> 列的数据，其产生的数据库表示<em>d&rsquo;</em> 对<em>S2</em> 是一致的，但对<em>S1</em> 是不一致的（有多余数据）。现在如果发起由<em>S1</em> 定义的<em>delete</em> 操作，<em>C</em> 列对应的数据就不能被正确删除了。</p><p>显然根据定义，我们有如下推论：<strong>Schema<em>S1</em> 至<em>S2</em> 的变更过程是保持一致的，当且仅当<em>S2</em> 至<em>S1</em> 的变更过程也是保持一致的。</strong></p><p>接下来我们可以得出如下结论：<strong>任何从Schema<em>S1</em> 至<em>S2</em> 的变更，如果其添加或删除了一项<em>public</em> Schema 元素<em>E</em> ，那么此变更不能保持一致性。</strong></p><p>我们先考虑添加<em>E</em> 的情况。不论<em>E</em> 是<em>table, column</em> 或<em>index</em> ，由<em>S2</em> 定义的<em>insert</em> 都将插入<em>S1</em> 未定义的数据，所以<em>S1</em> 至<em>S2</em> 的变更不能保持一致性。根据上面的“可逆”推论，删除的情况也得证。</p><p>接下来我们要证明：<strong>任何从Schema<em>S1</em> 至<em>S2</em> 的变更，如果其添加了一项<em>delete-only</em> Schema 元素<em>E</em> ，那么此变更过程保持一致。</strong></p><p>因为<em>S1</em> 和<em>S2</em> 上定义的任何操作都不会为<em>E</em> 创建数据，显然不会产生多余数据。又因为<em>E</em> 不处于<em>public</em> 状态，自然也不会破坏数据完整性。所以该变更保持一致性。</p><p>接下来我们要证明：<strong>任何从Schema<em>S1</em> 至<em>S2</em> 的变更，如果其将一项<em>delete-only</em> 状态的 Schema<em>optional</em> 元素<em>E</em> 置为<em>public</em> ，那么此变更过程保持一致。</strong></p><p>因为<em>S1</em> 和<em>S2</em> 上定义的<em>delete</em> 操作都能正确地删除<em>E</em> 所对应的 kv 对，不会产生多余数据。由于<em>S1</em> 中<em>E</em> 是<em>delete-only</em> ，<em>S1</em> 所定义的<em>insert</em> 不会为<em>E</em> 写入对应的数据，但是因为<em>E</em> 是<em>optional</em> 的，数据的缺失最终不会影响一致性。所以该变更过程保持一致。</p><p>到这里，我们就有了针对添加<em>optional</em> Schema 元素的完整变更方案：</p><pre><code>absent --&gt; delete-only --&gt; public</code></pre><p>删除 Schema 元素以及添加删除<em>required</em> 元素的情况都是类似的推导过程，这里就不再赘述了，具体可参考论文。</p><h2 id="其他">其他</h2><p>本文参考文献主要有：</p><ul><li>F1 简介<a href="http://research.google.com/pubs/archive/41344.pdf">F1: A Distributed SQL Database That Scales</a></li><li>Spanner 简介<a href="http://research.google.com/archive/spanner-osdi2012.pdf">Spanner: Google’s Globally-Distributed Database</a></li><li>Schema 变更算法<a href="http://research.google.com/pubs/archive/41376.pdf">Online, Asynchronous Schema Change in F1</a></li></ul><p>另，我们团队的开源项目<a href="https://github.com/pingcap/tidb">TiDB</a> 中已经有了一份在线 Schema 变更的初步实现，欢迎关注。</p>
]]></description></item></channel></rss>